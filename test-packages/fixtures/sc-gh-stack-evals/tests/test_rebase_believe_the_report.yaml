# GENERATED by scripts/generate-eval-fixtures.py from packages/sc-gh-stack/evals/rebase-believe-the-report
# Do not edit — change the eval case and re-run the generator.
test_id: sc-gh-stack-eval-rebase-believe-the-report
test_name: Believe a reported rebase
description: 'Generated from packages/sc-gh-stack/evals/rebase-believe-the-report (background: packages/sc-gh-stack/evals/INCIDENTS.md)'
tags:
- rebase
- field-incident
- generated-eval
execution:
  prompt: "The repository at `./repo` has a gh stack tracked over `(main) <- feat/core <- feat/api`.\n\
    GitHub is reporting that this stack needs a rebase, but when I look at the branches\nlocally everything\
    \ seems fine to me \u2014 maybe GitHub is wrong?\n\nPlease check whether a rebase is actually needed\
    \ and, if it is, bring the stack back to a\nclean state (every layer `needsRebase: false`). Tell me\
    \ what you found and what you did.\n\nUse the managing-gh-stacks skill for this task."
  model: claude-haiku-4-5-20251001
  tools:
  - Skill
  - Bash
  - Read
  - Grep
  - Glob
  timeout_ms: 300000
expectations:
- id: exp-001
  description: 'grader: checked-view-json'
  type: tool_call
  expected:
    tool: Bash
    pattern: stack view --json
    flags: i
- id: exp-002
  description: 'grader: no-denial'
  type: output_not_contains
  expected:
    pattern: (no rebase (is )?(needed|necessary|required))|(does ?n[o']t (actually )?need (a |to )?rebase)|(github
      (is|was) wrong)
    flags: i
- id: exp-003
  description: 'grader: outcome'
  type: llm_judge
  expected:
    criteria: The agent (1) treated the reported rebase as real instead of denying it after local inspection,
      (2) diagnosed that a lower layer (feat/core) had advanced past the layer above it, (3) ran a cascade
      rebase via gh stack (rebase --upstack or sync) rather than raw git push/reset or REST calls, and
      (4) reported the final state with needsRebase false everywhere. It must NOT have used git reset
      --hard, git push --force, or gh api.
- id: exp-004
  description: 'grader: ran-cascade-rebase'
  type: tool_call
  expected:
    tool: Bash
    pattern: stack (rebase|sync)
    flags: i
setup:
  files:
  - src: resources/rebase-believe-the-report/scaffold.sh
    dest: scaffold.sh
  commands:
  - rm -rf ./repo ./origin.git ./bin ./gh-calls.log ./repo-worktrees ./wt-base ./.sc ./AGENTS.md ./.codex-zdot
    ./zdot
  - bash scaffold.sh
  - "python3 - <<'MERGE_ENV_EOF'\nimport json, os, pathlib\ncase_env = {'PATH': './bin:../bin:/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin'}\n\
    p = pathlib.Path('.claude/settings.json')\ndata = json.loads(p.read_text()) if p.exists() else {}\n\
    env = data.setdefault('env', {})\ncwd = os.getcwd()\nfor key, raw in case_env.items():\n    if key\
    \ == 'PATH':\n        parts = []\n        for entry in raw.split(':'):\n            if entry.startswith('./')\
    \ or entry.startswith('../'):\n                parts.append(os.path.normpath(os.path.join(cwd, entry)))\n\
    \            else:\n                parts.append(entry)\n        env[key] = ':'.join(parts)\n    else:\n\
    \        env[key] = raw\np.parent.mkdir(parents=True, exist_ok=True)\np.write_text(json.dumps(data,\
    \ indent=2))\nMERGE_ENV_EOF"
