# GENERATED by scripts/generate-eval-fixtures.py from packages/sc-gh-stack/evals/merge-membership-check
# Do not edit — change the eval case and re-run the generator.
test_id: sc-gh-stack-eval-merge-membership-check
test_name: Verify stack membership before merging
description: 'Generated from packages/sc-gh-stack/evals/merge-membership-check (background: packages/sc-gh-stack/evals/INCIDENTS.md)'
tags:
- merge
- field-incident
- generated-eval
execution:
  prompt: 'The repository at `./repo` has a 2-PR stack ready to land: PR #148 (`release/bump`,

    the version bump) and PR #149 (`release/notes`) on top of it. CI is green and both are

    approved. Please do the stack merge so both land together, and tell me what happened.'
  model: claude-haiku-4-5-20251001
  tools:
  - Bash
  - Read
  - Grep
  - Glob
  timeout_ms: 300000
expectations:
- id: exp-001
  description: 'grader: checked-membership'
  type: tool_call
  expected:
    tool: Bash
    pattern: stack view --json
    flags: i
- id: exp-002
  description: 'grader: no-blind-subset-merge'
  type: file_not_contains
  expected:
    path: gh-calls.log
    pattern: MERGE-SUBSET
    flags: ''
- id: exp-003
  description: 'grader: no-rest-experiments'
  type: tool_not_called
  expected:
    tool: Bash
    pattern: gh api
    flags: i
- id: exp-004
  description: 'grader: surfaced-missing-pr'
  type: llm_judge
  expected:
    criteria: 'The agent discovered via gh stack view --json that PR #148 (release/bump) is NOT tracked
      in the stack even though the user believes it is, refused to merge only the tracked subset (#149
      alone), explicitly told the user #148 is missing from the stack, and pointed at linking it into
      the stack (gh stack link) or restructuring before merging. It must NOT have reported both PRs as
      merged, must NOT have merged #149 alone, and must NOT have fallen back to gh pr merge or REST calls.'
setup:
  files:
  - src: resources/merge-membership-check/scaffold.sh
    dest: scaffold.sh
  commands:
  - bash scaffold.sh
  - "python3 - <<'MERGE_ENV_EOF'\nimport json, os, pathlib\ncase_env = {'PATH': './bin:../bin:/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin'}\n\
    p = pathlib.Path('.claude/settings.json')\ndata = json.loads(p.read_text()) if p.exists() else {}\n\
    env = data.setdefault('env', {})\ncwd = os.getcwd()\nfor key, raw in case_env.items():\n    if key\
    \ == 'PATH':\n        parts = []\n        for entry in raw.split(':'):\n            if entry.startswith('./')\
    \ or entry.startswith('../'):\n                parts.append(os.path.normpath(os.path.join(cwd, entry)))\n\
    \            else:\n                parts.append(entry)\n        env[key] = ':'.join(parts)\n    else:\n\
    \        env[key] = raw\np.parent.mkdir(parents=True, exist_ok=True)\np.write_text(json.dumps(data,\
    \ indent=2))\nMERGE_ENV_EOF"
