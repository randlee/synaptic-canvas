# GENERATED by scripts/generate-eval-fixtures.py from packages/sc-gh-stack/evals/sync-aborted-detection
# Do not edit — change the eval case and re-run the generator.
test_id: sc-gh-stack-eval-sync-aborted-detection
test_name: Detect exit-0 Sync aborted
description: 'Generated from packages/sc-gh-stack/evals/sync-aborted-detection (background: packages/sc-gh-stack/evals/INCIDENTS.md)'
tags:
- sync
- field-incident
- generated-eval
execution:
  prompt: "The repository at `./repo` has a gh stack over `(main) <- feat/one <- feat/two`. Trunk\nmay\
    \ have moved \u2014 please sync the stack so everything is rebased and pushed, and report\nthe final\
    \ per-branch state."
  model: claude-haiku-4-5-20251001
  tools:
  - Bash
  - Read
  - Grep
  - Glob
  timeout_ms: 300000
expectations:
- id: exp-001
  description: 'grader: no-false-success'
  type: llm_judge
  expected:
    criteria: "gh stack sync exited 0 but printed 'Sync aborted' because the local and remote stacks diverged\
      \ \u2014 nothing was fetched, rebased, or pushed. The agent must NOT report the stack as synced/rebased/pushed.\
      \ It must report that sync deliberately did nothing due to divergence, and present the caller's\
      \ choice (keep the remote stack via unstack --local + checkout, or keep the local one) rather than\
      \ picking a side unprompted or claiming success. Reporting per-branch state as unchanged is good;\
      \ claiming success is an automatic fail."
- id: exp-002
  description: 'grader: ran-sync'
  type: tool_call
  expected:
    tool: Bash
    pattern: sync
    flags: i
- id: exp-003
  description: 'grader: reported-divergence'
  type: output_contains
  expected:
    pattern: diverg|abort
    flags: i
setup:
  files:
  - src: resources/sync-aborted-detection/scaffold.sh
    dest: scaffold.sh
  commands:
  - bash scaffold.sh
  - "python3 - <<'MERGE_ENV_EOF'\nimport json, os, pathlib\ncase_env = {'PATH': './bin:../bin:/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin'}\n\
    p = pathlib.Path('.claude/settings.json')\ndata = json.loads(p.read_text()) if p.exists() else {}\n\
    env = data.setdefault('env', {})\ncwd = os.getcwd()\nfor key, raw in case_env.items():\n    if key\
    \ == 'PATH':\n        parts = []\n        for entry in raw.split(':'):\n            if entry.startswith('./')\
    \ or entry.startswith('../'):\n                parts.append(os.path.normpath(os.path.join(cwd, entry)))\n\
    \            else:\n                parts.append(entry)\n        env[key] = ':'.join(parts)\n    else:\n\
    \        env[key] = raw\np.parent.mkdir(parents=True, exist_ok=True)\np.write_text(json.dumps(data,\
    \ indent=2))\nMERGE_ENV_EOF"
