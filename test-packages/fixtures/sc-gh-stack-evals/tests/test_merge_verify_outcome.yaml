# GENERATED by scripts/generate-eval-fixtures.py from packages/sc-gh-stack/evals/merge-verify-outcome
# Do not edit — change the eval case and re-run the generator.
test_id: sc-gh-stack-eval-merge-verify-outcome
test_name: Merge and verify atomically
description: 'Generated from packages/sc-gh-stack/evals/merge-verify-outcome (background: packages/sc-gh-stack/evals/INCIDENTS.md)'
tags:
- merge
- field-incident
- generated-eval
execution:
  prompt: "The repository at `./repo` has a 2-PR release stack ready to land: PR #148\n(`release/bump`,\
    \ sets VERSION to 0.6.0) at the bottom and PR #149 (`release/notes`) on\ntop. CI is green and both\
    \ are approved. Land the stack and confirm the release actually\nhappened \u2014 I need to know `main`\
    \ really carries 0.6.0 before I tag."
  model: claude-haiku-4-5-20251001
  tools:
  - Bash
  - Read
  - Grep
  - Glob
  timeout_ms: 300000
expectations:
- id: exp-001
  description: 'grader: checked-membership-first'
  type: tool_order
  expected:
    before: stack view --json
    after: stack merge
- id: exp-002
  description: 'grader: verified-merged-state'
  type: file_contains
  expected:
    path: gh-calls.log
    pattern: gh (pr view 14[89]|stack view)
    flags: ''
- id: exp-003
  description: 'grader: verified-version-on-main'
  type: llm_judge
  expected:
    criteria: 'The agent (1) confirmed stack membership (#148 and #149 both tracked, in order) via gh
      stack view --json BEFORE merging, (2) merged via gh stack merge (never gh pr merge or REST), (3)
      after merging, verified BOTH PRs report MERGED and that main actually carries VERSION 0.6.0 (e.g.
      git show origin/main:VERSION or fetching and reading the file), and (4) only then reported success,
      stating 0.6.0 is on main. Reporting success without the post-merge verification, or without checking
      VERSION on main, is a fail. Efficiency counts: the core flow should be roughly view -> merge ->
      verify, not long exploratory polling.'
setup:
  files:
  - src: resources/merge-verify-outcome/scaffold.sh
    dest: scaffold.sh
  commands:
  - bash scaffold.sh
  - "python3 - <<'MERGE_ENV_EOF'\nimport json, os, pathlib\ncase_env = {'PATH': './bin:../bin:/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin'}\n\
    p = pathlib.Path('.claude/settings.json')\ndata = json.loads(p.read_text()) if p.exists() else {}\n\
    env = data.setdefault('env', {})\ncwd = os.getcwd()\nfor key, raw in case_env.items():\n    if key\
    \ == 'PATH':\n        parts = []\n        for entry in raw.split(':'):\n            if entry.startswith('./')\
    \ or entry.startswith('../'):\n                parts.append(os.path.normpath(os.path.join(cwd, entry)))\n\
    \            else:\n                parts.append(entry)\n        env[key] = ':'.join(parts)\n    else:\n\
    \        env[key] = raw\np.parent.mkdir(parents=True, exist_ok=True)\np.write_text(json.dumps(data,\
    \ indent=2))\nMERGE_ENV_EOF"
