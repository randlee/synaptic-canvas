# GENERATED by scripts/generate-eval-fixtures.py from packages/sc-gh-stack/evals/release-stack-shape
# Do not edit — change the eval case and re-run the generator.
test_id: sc-gh-stack-eval-release-stack-shape
test_name: Release stack shape
description: 'Generated from packages/sc-gh-stack/evals/release-stack-shape (background: packages/sc-gh-stack/evals/INCIDENTS.md)'
tags:
- plan
- field-incident
- generated-eval
execution:
  prompt: "I'm cutting release 0.6.0. The flow is: the branch `release/0.6.0` (version bump +\nchangelog)\
    \ must land in `develop`, and then `develop` must land in `main`. Both `develop`\nand `main` are protected\
    \ branches with required CI.\n\nMy plan: make a two-layer gh stack \u2014 bottom layer the `release/0.6.0\
    \ -> develop` PR, top\nlayer the `develop -> main` PR \u2014 and land the whole thing with one `gh\
    \ stack merge --yes`.\n\nIs that the right shape? If not, what should I do instead?"
  model: claude-haiku-4-5-20251001
  tools:
  - Skill
  - Read
  - Grep
  - Glob
  timeout_ms: 180000
expectations:
- id: exp-001
  description: 'grader: no-endorsement'
  type: output_not_contains
  expected:
    pattern: (yes, that('s| is) (the )?right|your plan is correct|that shape works)
    flags: i
- id: exp-002
  description: 'grader: rejects-protected-head-layer'
  type: llm_judge
  expected:
    criteria: "The agent rejected the proposed shape (release/0.6.0 -> develop BENEATH develop -> main)\
      \ for the correct structural reason: the lower layer merges INTO develop, mutating the upper layer's\
      \ protected head mid-cascade and invalidating its gated CI from inside the stack (and/or: stack\
      \ tooling cannot rebase a protected head). It proposed a correct alternative \u2014 any of: (a)\
      \ land release/0.6.0 -> develop first, then the develop -> main PR separately; (b) a single release/0.6.0\
      \ -> main PR plus a merge-forward to develop; or (c) the CARRY-LAYER shape: a main-based stack (main\
      \ <- develop <- release/0.6.0) where develop sits at the BOTTOM with nothing below feeding it, everything\
      \ lands on main via the retarget cascade, and main -> develop merge-forwards afterward. Endorsing\
      \ the two-layer stack AS PROPOSED (develop -> main on top) is a fail. Rejecting protected-head layers\
      \ categorically without the fed-from-below distinction is acceptable but weaker \u2014 do not fail\
      \ it for that alone; fail only wrong reasons or endorsement of the proposed shape."
