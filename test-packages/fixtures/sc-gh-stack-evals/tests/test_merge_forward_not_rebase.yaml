# GENERATED by scripts/generate-eval-fixtures.py from packages/sc-gh-stack/evals/merge-forward-not-rebase
# Do not edit — change the eval case and re-run the generator.
test_id: sc-gh-stack-eval-merge-forward-not-rebase
test_name: Merge-forward, not rebase
description: 'Generated from packages/sc-gh-stack/evals/merge-forward-not-rebase (background: packages/sc-gh-stack/evals/INCIDENTS.md)'
tags:
- rebase
- field-incident
- generated-eval
execution:
  prompt: "The repository at `./repo` has a gh stack based on `develop`\n(`(develop) <- feat/lint <- feat/docs`).\
    \ Something seems out of date \u2014 a teammate said\nthe stack \"needs updating from main\". Please\
    \ figure out what is actually going on and\neither fix it or tell me exactly what to do. Check `CONTRIBUTING.md`\
    \ for the repo's\nbranch policy first.\n\nUse the managing-gh-stacks skill for this task."
  model: claude-haiku-4-5-20251001
  tools:
  - Skill
  - Bash
  - Read
  - Grep
  - Glob
  timeout_ms: 300000
expectations:
- id: exp-001
  description: 'grader: diagnosed-with-revlist'
  type: tool_call
  expected:
    tool: Bash
    pattern: rev-list|merge-base|log .*develop.*main|log .*main.*develop
    flags: i
- id: exp-002
  description: 'grader: no-rebase-attempt'
  type: file_not_contains
  expected:
    path: gh-calls.log
    pattern: (REBASE|SYNC)-ATTEMPTED
    flags: ''
- id: exp-003
  description: 'grader: recommended-merge-forward'
  type: llm_judge
  expected:
    criteria: The agent (1) read the merge-forward-only policy, (2) determined that main has a commit
      develop lacks (drift ABOVE the stack's base) rather than the stack being stale against develop,
      (3) did NOT run or recommend gh stack rebase/sync for it, and (4) recommended or opened a merge-forward
      PR (main -> develop) as the fix. Recommending a rebase of any branch onto main, or telling the user
      the stack itself needs rebasing, is a fail.
setup:
  files:
  - src: resources/merge-forward-not-rebase/scaffold.sh
    dest: scaffold.sh
  commands:
  - rm -rf ./repo ./origin.git ./bin ./gh-calls.log ./repo-worktrees ./wt-base ./.sc ./AGENTS.md ./.codex-zdot
    ./zdot
  - bash scaffold.sh
  - "python3 - <<'MERGE_ENV_EOF'\nimport json, os, pathlib\ncase_env = {'PATH': './bin:../bin:/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin'}\n\
    p = pathlib.Path('.claude/settings.json')\ndata = json.loads(p.read_text()) if p.exists() else {}\n\
    env = data.setdefault('env', {})\ncwd = os.getcwd()\nfor key, raw in case_env.items():\n    if key\
    \ == 'PATH':\n        parts = []\n        for entry in raw.split(':'):\n            if entry.startswith('./')\
    \ or entry.startswith('../'):\n                parts.append(os.path.normpath(os.path.join(cwd, entry)))\n\
    \            else:\n                parts.append(entry)\n        env[key] = ':'.join(parts)\n    else:\n\
    \        env[key] = raw\np.parent.mkdir(parents=True, exist_ok=True)\np.write_text(json.dumps(data,\
    \ indent=2))\nMERGE_ENV_EOF"
