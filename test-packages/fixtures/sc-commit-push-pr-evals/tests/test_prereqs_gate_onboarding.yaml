# GENERATED by scripts/generate-eval-fixtures.py from packages/sc-commit-push-pr/evals/prereqs-gate-onboarding
# Do not edit — change the eval case and re-run the generator.
test_id: sc-commit-push-pr-eval-prereqs-gate-onboarding
test_name: Missing gh-stack toolchain blocks with install instructions
description: 'Generated from packages/sc-commit-push-pr/evals/prereqs-gate-onboarding (background: packages/sc-commit-push-pr/evals/INCIDENTS.md)'
tags:
- prereqs
- onboarding
- field-incident
- generated-eval
execution:
  prompt: "The repository at `./repo` is on branch `feature/x` with a staged change ready to go.\nPlease\
    \ commit the staged change and open a PR to `develop`.\n\nInvoke the sc-commit-push-pr skill and use\
    \ its packaged pipeline scripts (.claude/scripts/) \u2014 do not hand-roll git push or gh pr create.\n\
    \nRun every command in the foreground; never run commands or tasks in the background."
  model: claude-haiku-4-5-20251001
  tools:
  - Skill
  - Bash
  - Read
  - Grep
  - Glob
  timeout_ms: 300000
expectations:
- id: exp-001
  description: 'grader: exact-extension-line'
  type: output_contains
  expected:
    pattern: gh extension install github/gh-stack
    flags: ''
- id: exp-002
  description: 'grader: exact-plugin-line'
  type: output_contains
  expected:
    pattern: /plugin install sc-gh-stack
    flags: ''
- id: exp-003
  description: 'grader: no-mutation-around-gate'
  type: tool_not_called
  expected:
    tool: Bash
    pattern: git push|gh pr create
    flags: i
- id: exp-004
  description: 'grader: tells-user-to-install'
  type: llm_judge
  expected:
    criteria: The agent ran (or attempted to run) the sc-commit-push-pr pipeline under .claude/scripts,
      discovered the gh-stack toolchain prerequisites are not met (PREFLIGHT.STACK_PREREQS_MISSING --
      specifically the gh-stack extension and the managing-gh-stacks skill are both missing), and reported
      this to the user clearly, listing the exact install steps. It must NOT have attempted to push the
      branch or create a PR before or instead of surfacing the gate, and must NOT have tried to improvise
      a fix itself (e.g. hand-installing gh-stack from source, silently skipping the check, or reasoning
      that the gate 'probably doesn't matter' for this branch since it isn't a stack layer -- the gate
      is unconditional). Telling the user to run the listed install commands and stopping there is the
      correct, complete behavior.
setup:
  files:
  - src: resources/prereqs-gate-onboarding/scaffold.sh
    dest: scaffold.sh
  commands:
  - bash scaffold.sh
  - "python3 - <<'MERGE_ENV_EOF'\nimport json, os, pathlib\ncase_env = {'PATH': './bin:../bin:/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin'}\n\
    p = pathlib.Path('.claude/settings.json')\ndata = json.loads(p.read_text()) if p.exists() else {}\n\
    env = data.setdefault('env', {})\ncwd = os.getcwd()\nfor key, raw in case_env.items():\n    if key\
    \ == 'PATH':\n        parts = []\n        for entry in raw.split(':'):\n            if entry.startswith('./')\
    \ or entry.startswith('../'):\n                parts.append(os.path.normpath(os.path.join(cwd, entry)))\n\
    \            else:\n                parts.append(entry)\n        env[key] = ':'.join(parts)\n    else:\n\
    \        env[key] = raw\np.parent.mkdir(parents=True, exist_ok=True)\np.write_text(json.dumps(data,\
    \ indent=2))\nMERGE_ENV_EOF"
