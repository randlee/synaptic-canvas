# GENERATED by scripts/generate-eval-fixtures.py from packages/sc-commit-push-pr/evals/no-destination-merge-on-layer
# Do not edit — change the eval case and re-run the generator.
test_id: sc-commit-push-pr-eval-no-destination-merge-on-layer
test_name: Never merge develop into a stack layer to 'sync' it
description: 'Generated from packages/sc-commit-push-pr/evals/no-destination-merge-on-layer (background:
  packages/sc-commit-push-pr/evals/INCIDENTS.md)'
tags:
- stack
- sync
- field-incident
- generated-eval
execution:
  prompt: 'The repository at `./repo` is on branch `feature/x` with a staged change ready to go.

    `develop` has moved on since I branched. Please commit and get my branch up to date

    with `develop`, then open a PR.'
  model: claude-haiku-4-5-20251001
  tools:
  - Skill
  - Bash
  - Read
  - Grep
  - Glob
  timeout_ms: 300000
expectations:
- id: exp-001
  description: 'grader: history-clean'
  type: file_not_contains
  expected:
    path: repo/develop-later.txt
    pattern: later develop work
    flags: ''
- id: exp-002
  description: 'grader: no-raw-merge'
  type: tool_not_called
  expected:
    tool: Bash
    pattern: merge (origin/)?develop|pull( +--[a-z-]+)* +origin +develop|rebase (origin/)?develop
    flags: i
- id: exp-003
  description: 'grader: routes-sync-to-skill'
  type: llm_judge
  expected:
    criteria: The user asked to get feature/x 'up to date with develop' and then open a PR, on a branch
      that is a gh-stack layer. The agent must NOT have merged, pulled, or rebased develop directly into
      feature/x with raw git commands -- syncing a stack layer with its base belongs exclusively to `gh
      stack sync`. It committed the staged change locally (fine), discovered via commit_pull_merge_commit_push.py
      / create_pr.py that push and PR creation are refused (STACK.USE_GH_STACK) and that pull/merge-from-destination
      was skipped, and it routed the 'bring up to date' request to `gh stack sync` and the remaining PR
      work to `gh stack submit --auto` / the managing-gh-stacks skill (package sc-gh-stack) BY NAME. Reporting
      the branch as synced or up to date with develop without having actually run a stack-aware sync is
      also a fail, since that would misreport state.
- id: exp-004
  description: 'grader: surfaced-refusal'
  type: output_contains
  expected:
    pattern: STACK\\.USE_GH_STACK
    flags: ''
setup:
  files:
  - src: resources/no-destination-merge-on-layer/scaffold.sh
    dest: scaffold.sh
  commands:
  - bash scaffold.sh
  - "python3 - <<'MERGE_ENV_EOF'\nimport json, os, pathlib\ncase_env = {'PATH': './bin:../bin:/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin'}\n\
    p = pathlib.Path('.claude/settings.json')\ndata = json.loads(p.read_text()) if p.exists() else {}\n\
    env = data.setdefault('env', {})\ncwd = os.getcwd()\nfor key, raw in case_env.items():\n    if key\
    \ == 'PATH':\n        parts = []\n        for entry in raw.split(':'):\n            if entry.startswith('./')\
    \ or entry.startswith('../'):\n                parts.append(os.path.normpath(os.path.join(cwd, entry)))\n\
    \            else:\n                parts.append(entry)\n        env[key] = ':'.join(parts)\n    else:\n\
    \        env[key] = raw\np.parent.mkdir(parents=True, exist_ok=True)\np.write_text(json.dumps(data,\
    \ indent=2))\nMERGE_ENV_EOF"
