# GENERATED by scripts/generate-eval-fixtures.py from packages/sc-commit-push-pr/evals/non-stack-flow-normal
# Do not edit — change the eval case and re-run the generator.
test_id: sc-commit-push-pr-eval-non-stack-flow-normal
test_name: Ordinary commit/push/PR flow (no stack)
description: 'Generated from packages/sc-commit-push-pr/evals/non-stack-flow-normal (background: packages/sc-commit-push-pr/evals/INCIDENTS.md)'
tags:
- baseline
- non-stack
- generated-eval
execution:
  prompt: "The repository at `./repo` is on branch `feature/x` with a staged change ready to go.\nPlease\
    \ commit the staged change and open a PR to `develop`.\n\nInvoke the sc-commit-push-pr skill and use\
    \ its packaged pipeline scripts (.claude/scripts/) \u2014 do not hand-roll git push or gh pr create.\n\
    \nRun every command in the foreground; never run commands or tasks in the background."
  model: claude-haiku-4-5-20251001
  tools:
  - Skill
  - Bash
  - Read
  - Grep
  - Glob
  timeout_ms: 300000
expectations:
- id: exp-001
  description: 'grader: flow-completed'
  type: file_contains
  expected:
    path: gh-calls.log
    pattern: PR-CREATED
    flags: ''
- id: exp-002
  description: 'grader: no-gh-stack-commands'
  type: tool_not_called
  expected:
    tool: Bash
    pattern: gh stack
    flags: i
- id: exp-003
  description: 'grader: no-stack-refusal'
  type: output_not_contains
  expected:
    pattern: STACK\\.USE_GH_STACK|PREFLIGHT\\.STACK_PREREQS_MISSING
    flags: i
setup:
  files:
  - src: resources/non-stack-flow-normal/scaffold.sh
    dest: scaffold.sh
  commands:
  - bash scaffold.sh
  - "python3 - <<'MERGE_ENV_EOF'\nimport json, os, pathlib\ncase_env = {'PATH': './bin:../bin:/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin'}\n\
    p = pathlib.Path('.claude/settings.json')\ndata = json.loads(p.read_text()) if p.exists() else {}\n\
    env = data.setdefault('env', {})\ncwd = os.getcwd()\nfor key, raw in case_env.items():\n    if key\
    \ == 'PATH':\n        parts = []\n        for entry in raw.split(':'):\n            if entry.startswith('./')\
    \ or entry.startswith('../'):\n                parts.append(os.path.normpath(os.path.join(cwd, entry)))\n\
    \            else:\n                parts.append(entry)\n        env[key] = ':'.join(parts)\n    else:\n\
    \        env[key] = raw\np.parent.mkdir(parents=True, exist_ok=True)\np.write_text(json.dumps(data,\
    \ indent=2))\nMERGE_ENV_EOF"
