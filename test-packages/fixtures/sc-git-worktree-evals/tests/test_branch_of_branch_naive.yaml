# GENERATED by scripts/generate-eval-fixtures.py from packages/sc-git-worktree/evals/branch-of-branch-naive
# Do not edit — change the eval case and re-run the generator.
test_id: sc-git-worktree-eval-branch-of-branch-naive
test_name: Branch-of-branch stays flat in a stack-naive repo
description: 'Generated from packages/sc-git-worktree/evals/branch-of-branch-naive (background: packages/sc-git-worktree/evals/INCIDENTS.md)'
tags:
- create
- decision-model
- generated-eval
execution:
  prompt: 'The repository at `./repo` has a branch `feat/base` that is not yet merged

    into `main`. Please create a worktree for `feature/child` based on

    `feat/base`, and tell me where it landed.


    Run every command in the foreground; never run commands or tasks in the background.'
  model: claude-haiku-4-5-20251001
  tools:
  - Skill
  - Bash
  - Read
  - Grep
  - Glob
  timeout_ms: 300000
expectations:
- id: exp-001
  description: 'grader: no-stack-ops'
  type: file_not_contains
  expected:
    path: gh-calls.log
    pattern: stack
    flags: i
- id: exp-002
  description: 'grader: succeeded-flat-no-refusal'
  type: llm_judge
  expected:
    criteria: 'The agent successfully created a worktree for feature/child based on feat/base, an unmerged
      branch. It must NOT have refused, asked for a `flat: true` override, warned that a new stack was
      needed, or treated the unmerged base as a dependency signal in any way - this repo is stack-naive
      (no always_stack policy, no worktree anywhere carries gh-stack tracking), so a branch-of-branch
      create must behave exactly as it did before gh-stack support existed (the distribution guarantee
      / auto-upgrade for legacy prompts).'
- id: exp-003
  description: 'grader: worktree-created'
  type: file_contains
  expected:
    path: repo-worktrees/feature/child/base.txt
    pattern: on-base
    flags: ''
setup:
  files:
  - src: resources/branch-of-branch-naive/scaffold.sh
    dest: scaffold.sh
  commands:
  - rm -rf ./repo ./origin.git ./bin ./gh-calls.log ./repo-worktrees ./wt-base ./.sc ./AGENTS.md ./.codex-zdot
    ./zdot
  - bash scaffold.sh
  - "python3 - <<'MERGE_ENV_EOF'\nimport json, os, pathlib\ncase_env = {'PATH': './bin:../bin:/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin'}\n\
    p = pathlib.Path('.claude/settings.json')\ndata = json.loads(p.read_text()) if p.exists() else {}\n\
    env = data.setdefault('env', {})\ncwd = os.getcwd()\nfor key, raw in case_env.items():\n    if key\
    \ == 'PATH':\n        parts = []\n        for entry in raw.split(':'):\n            if entry.startswith('./')\
    \ or entry.startswith('../'):\n                parts.append(os.path.normpath(os.path.join(cwd, entry)))\n\
    \            else:\n                parts.append(entry)\n        env[key] = ':'.join(parts)\n    else:\n\
    \        env[key] = raw\np.parent.mkdir(parents=True, exist_ok=True)\np.write_text(json.dumps(data,\
    \ indent=2))\nMERGE_ENV_EOF"
