# GENERATED by scripts/generate-eval-fixtures.py from packages/sc-git-worktree/evals/dependent-base-joins-stack
# Do not edit — change the eval case and re-run the generator.
test_id: sc-git-worktree-eval-dependent-base-joins-stack
test_name: Dependent base joins the existing stack as a layer
description: 'Generated from packages/sc-git-worktree/evals/dependent-base-joins-stack (background: packages/sc-git-worktree/evals/INCIDENTS.md)'
tags:
- create
- decision-model
- generated-eval
execution:
  prompt: "The repository at `./repo` already has `feat/bottom` checked out in its own\nworktree (`./repo-worktrees/feat/bottom`),\
    \ tracked as a gh-stack. Please\ncreate a worktree for `feature/next` based on `feat/bottom`, and\
    \ tell me\nwhere the work ended up.\n\nInvoke the sc-git-worktree skill and use its packaged scripts\
    \ (.claude/scripts/worktree_*.py) \u2014 do not improvise raw git worktree commands.\n\nRun every\
    \ command in the foreground; never run commands or tasks in the background."
  model: claude-haiku-4-5-20251001
  tools:
  - Skill
  - Bash
  - Read
  - Grep
  - Glob
  timeout_ms: 300000
expectations:
- id: exp-001
  description: 'grader: names-existing-worktree'
  type: llm_judge
  expected:
    criteria: The agent correctly determined feat/bottom is a dependent (unmerged) base that already has
      a tracked gh-stack worktree, and joined feature/next into that EXISTING worktree (./repo-worktrees/feat/bottom)
      as a new layer rather than creating a separate worktree for feature/next. The final message must
      name the existing stack worktree as where the work now lives (e.g. that feature/next is checked
      out inside repo-worktrees/feat/bottom). It must NOT claim a new worktree directory was created for
      feature/next, and must NOT refuse the request.
- id: exp-002
  description: 'grader: no-new-worktree-dir'
  type: tool_not_called
  expected:
    tool: Bash
    pattern: worktree add[^\\n]*feature/next
    flags: i
- id: exp-003
  description: 'grader: stack-add-ran'
  type: file_contains
  expected:
    path: gh-calls.log
    pattern: stack add feature/next
    flags: ''
setup:
  files:
  - src: resources/dependent-base-joins-stack/scaffold.sh
    dest: scaffold.sh
  commands:
  - bash scaffold.sh
  - "python3 - <<'MERGE_ENV_EOF'\nimport json, os, pathlib\ncase_env = {'PATH': './bin:../bin:/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin'}\n\
    p = pathlib.Path('.claude/settings.json')\ndata = json.loads(p.read_text()) if p.exists() else {}\n\
    env = data.setdefault('env', {})\ncwd = os.getcwd()\nfor key, raw in case_env.items():\n    if key\
    \ == 'PATH':\n        parts = []\n        for entry in raw.split(':'):\n            if entry.startswith('./')\
    \ or entry.startswith('../'):\n                parts.append(os.path.normpath(os.path.join(cwd, entry)))\n\
    \            else:\n                parts.append(entry)\n        env[key] = ':'.join(parts)\n    else:\n\
    \        env[key] = raw\np.parent.mkdir(parents=True, exist_ok=True)\np.write_text(json.dumps(data,\
    \ indent=2))\nMERGE_ENV_EOF"
