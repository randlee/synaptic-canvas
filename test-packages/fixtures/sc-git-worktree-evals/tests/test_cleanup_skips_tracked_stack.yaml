# GENERATED by scripts/generate-eval-fixtures.py from packages/sc-git-worktree/evals/cleanup-skips-tracked-stack
# Do not edit — change the eval case and re-run the generator.
test_id: sc-git-worktree-eval-cleanup-skips-tracked-stack
test_name: Batch cleanup skips gh-stack-tracked worktrees
description: 'Generated from packages/sc-git-worktree/evals/cleanup-skips-tracked-stack (background: packages/sc-git-worktree/evals/INCIDENTS.md)'
tags:
- cleanup
- decision-model
- safety
- generated-eval
execution:
  prompt: 'The repository at `./repo` has two worktrees: `feature/tracked-stack` and

    `feature/merged-plain`. Please clean up all merged worktrees and tell me what

    happened.'
  model: claude-haiku-4-5-20251001
  tools:
  - Skill
  - Bash
  - Read
  - Grep
  - Glob
  timeout_ms: 300000
expectations:
- id: exp-001
  description: 'grader: merged-plain-cleaned'
  type: tool_call
  expected:
    tool: Bash
    pattern: merged-plain
    flags: i
- id: exp-002
  description: 'grader: no-removal-of-tracked'
  type: tool_not_called
  expected:
    tool: Bash
    pattern: worktree remove[^\\n]*tracked-stack
    flags: i
- id: exp-003
  description: 'grader: surfaces-skip-reason'
  type: llm_judge
  expected:
    criteria: 'The agent''s final report distinguishes the two worktrees: feature/merged-plain was cleaned
      up normally (worktree and branches removed), while feature/tracked-stack was explicitly skipped/preserved
      because it carries gh-stack tracking (per-worktree stack state that batch cleanup must not silently
      remove). It must NOT claim both were cleaned up, and must NOT claim feature/tracked-stack was removed.'
- id: exp-004
  description: 'grader: tracked-worktree-survives'
  type: file_contains
  expected:
    path: repo-worktrees/feature/tracked-stack/README.md
    pattern: init
    flags: ''
setup:
  files:
  - src: resources/cleanup-skips-tracked-stack/scaffold.sh
    dest: scaffold.sh
  commands:
  - bash scaffold.sh
  - "python3 - <<'MERGE_ENV_EOF'\nimport json, os, pathlib\ncase_env = {'PATH': './bin:../bin:/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin'}\n\
    p = pathlib.Path('.claude/settings.json')\ndata = json.loads(p.read_text()) if p.exists() else {}\n\
    env = data.setdefault('env', {})\ncwd = os.getcwd()\nfor key, raw in case_env.items():\n    if key\
    \ == 'PATH':\n        parts = []\n        for entry in raw.split(':'):\n            if entry.startswith('./')\
    \ or entry.startswith('../'):\n                parts.append(os.path.normpath(os.path.join(cwd, entry)))\n\
    \            else:\n                parts.append(entry)\n        env[key] = ':'.join(parts)\n    else:\n\
    \        env[key] = raw\np.parent.mkdir(parents=True, exist_ok=True)\np.write_text(json.dumps(data,\
    \ indent=2))\nMERGE_ENV_EOF"
