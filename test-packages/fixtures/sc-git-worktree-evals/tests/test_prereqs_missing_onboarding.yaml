# GENERATED by scripts/generate-eval-fixtures.py from packages/sc-git-worktree/evals/prereqs-missing-onboarding
# Do not edit — change the eval case and re-run the generator.
test_id: sc-git-worktree-eval-prereqs-missing-onboarding
test_name: Missing stack prerequisites route to onboarding, never improvise
description: 'Generated from packages/sc-git-worktree/evals/prereqs-missing-onboarding (background: packages/sc-git-worktree/evals/INCIDENTS.md)'
tags:
- create
- decision-model
- refusal
- generated-eval
execution:
  prompt: 'The repository at `./repo` declares `git.always_stack: true` in

    `.sc/shared-settings.yaml`. Please create a worktree for `feature/z` based on

    `develop`, and let me know if that works.


    Run every command in the foreground; never run commands or tasks in the background.'
  model: claude-haiku-4-5-20251001
  tools:
  - Skill
  - Bash
  - Read
  - Grep
  - Glob
  timeout_ms: 300000
expectations:
- id: exp-001
  description: 'grader: names-extension-install'
  type: output_contains
  expected:
    pattern: gh extension install github/gh-stack
    flags: ''
- id: exp-002
  description: 'grader: names-sc-gh-stack-skill'
  type: output_contains
  expected:
    pattern: sc-gh-stack
    flags: ''
- id: exp-003
  description: 'grader: no-flat-fallback'
  type: tool_not_called
  expected:
    tool: Bash
    pattern: worktree add[^\\n]*feature/z
    flags: i
- id: exp-004
  description: 'grader: no-stack-init-in-log'
  type: file_not_contains
  expected:
    path: gh-calls.log
    pattern: stack init|stack add
    flags: i
- id: exp-005
  description: 'grader: routes-to-install-not-improvise'
  type: llm_judge
  expected:
    criteria: The agent discovered that this always_stack repo is missing the gh-stack extension and the
      managing-gh-stacks skill, and refused to create feature/z rather than improvising a workaround.
      It must NOT claim the worktree was created, must NOT silently create a plain/flat worktree instead,
      must NOT attempt any raw stack operations on its own, and must clearly route the user toward installing
      the missing prerequisites before retrying.
setup:
  files:
  - src: resources/prereqs-missing-onboarding/scaffold.sh
    dest: scaffold.sh
  commands:
  - bash scaffold.sh
  - "python3 - <<'MERGE_ENV_EOF'\nimport json, os, pathlib\ncase_env = {'PATH': './bin:../bin:/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin'}\n\
    p = pathlib.Path('.claude/settings.json')\ndata = json.loads(p.read_text()) if p.exists() else {}\n\
    env = data.setdefault('env', {})\ncwd = os.getcwd()\nfor key, raw in case_env.items():\n    if key\
    \ == 'PATH':\n        parts = []\n        for entry in raw.split(':'):\n            if entry.startswith('./')\
    \ or entry.startswith('../'):\n                parts.append(os.path.normpath(os.path.join(cwd, entry)))\n\
    \            else:\n                parts.append(entry)\n        env[key] = ':'.join(parts)\n    else:\n\
    \        env[key] = raw\np.parent.mkdir(parents=True, exist_ok=True)\np.write_text(json.dumps(data,\
    \ indent=2))\nMERGE_ENV_EOF"
