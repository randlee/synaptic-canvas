# GENERATED by scripts/generate-eval-fixtures.py from packages/sc-git-worktree/evals/always-stack-product-b
# Do not edit — change the eval case and re-run the generator.
test_id: sc-git-worktree-eval-always-stack-product-b
test_name: always_stack policy produces a new stack
description: 'Generated from packages/sc-git-worktree/evals/always-stack-product-b (background: packages/sc-git-worktree/evals/INCIDENTS.md)'
tags:
- create
- decision-model
- generated-eval
execution:
  prompt: "The repository at `./repo` uses gh-stack for everything (see\n`.sc/shared-settings.yaml`).\
    \ Please create a worktree for `feature/y` based on\n`develop`, and tell me what happened, including\
    \ whether it's tracked as a\nstack.\n\nInvoke the sc-git-worktree skill and use its packaged scripts\
    \ (.claude/scripts/worktree_*.py) \u2014 do not improvise raw git worktree commands.\n\nRun every\
    \ command in the foreground; never run commands or tasks in the background."
  model: claude-haiku-4-5-20251001
  tools:
  - Skill
  - Bash
  - Read
  - Grep
  - Glob
  timeout_ms: 300000
expectations:
- id: exp-001
  description: 'grader: output-mentions-stack'
  type: llm_judge
  expected:
    criteria: 'The agent created a worktree for feature/y based on develop and communicated that it IS
      tracked as a gh-stack (mentions gh stack init, stack tracking, or being part of a stack), because
      .sc/shared-settings.yaml declares git.always_stack: true. It must NOT describe a plain/flat worktree
      with no stack involvement, and must NOT describe a refusal or missing-prerequisite error - all prerequisites
      are present in this fixture.'
- id: exp-002
  description: 'grader: same-path-no-stack-prefix'
  type: output_not_contains
  expected:
    pattern: worktrees/stack/
    flags: ''
- id: exp-003
  description: 'grader: stack-init-ran'
  type: file_contains
  expected:
    path: gh-calls.log
    pattern: stack init --base develop
    flags: ''
setup:
  files:
  - src: resources/always-stack-product-b/scaffold.sh
    dest: scaffold.sh
  commands:
  - rm -rf ./repo ./origin.git ./bin ./gh-calls.log ./repo-worktrees ./wt-base ./.sc ./AGENTS.md ./.codex-zdot
    ./zdot
  - bash scaffold.sh
  - "python3 - <<'MERGE_ENV_EOF'\nimport json, os, pathlib\ncase_env = {'PATH': './bin:../bin:/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin'}\n\
    p = pathlib.Path('.claude/settings.json')\ndata = json.loads(p.read_text()) if p.exists() else {}\n\
    env = data.setdefault('env', {})\ncwd = os.getcwd()\nfor key, raw in case_env.items():\n    if key\
    \ == 'PATH':\n        parts = []\n        for entry in raw.split(':'):\n            if entry.startswith('./')\
    \ or entry.startswith('../'):\n                parts.append(os.path.normpath(os.path.join(cwd, entry)))\n\
    \            else:\n                parts.append(entry)\n        env[key] = ':'.join(parts)\n    else:\n\
    \        env[key] = raw\np.parent.mkdir(parents=True, exist_ok=True)\np.write_text(json.dumps(data,\
    \ indent=2))\nMERGE_ENV_EOF"
