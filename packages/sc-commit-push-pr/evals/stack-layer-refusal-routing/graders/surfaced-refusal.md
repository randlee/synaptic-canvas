---
type: regex
pattern: "STACK\\.USE_GH_STACK"
flags: ""
match: contains
target: last_message
---

The final report must name the actual refusal code -- not silently retry, not
report success, not invent a different explanation.
