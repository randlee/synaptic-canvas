# Use Cases

## Share a feature subtree offline

Export a feature bead and its descendants to markdown so a reviewer can read the plan without direct board access.

## Build a browser-viewable handoff

Generate the optional HTML site when a stakeholder wants a simple local viewer instead of raw markdown.

## Package a clean archive

Create a zip when the export needs to be attached to a ticket, uploaded to storage, or shared across systems.
